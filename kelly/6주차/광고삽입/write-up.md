## 👀 제한 시간 안에 어디까지 해냈는가?

제한 시간 `30분`안에 문제를 해결하지 못했다. 아예 아이디어 조차 떠올리지 못했다.

---

## 🧑‍🔬 문제 분석

총 영상 재생 시간, 총 광고 재생 시간, 사용자들의 재생 구간 정보 리스트가 주어졌을 때 제일 많은 사람들이 광고를 시청할 수 있는 구간의 시작 시간을 반환하는 문제이다.

- 문제 조건이 꽤나 복잡해서 자세한건 문제를 참고하자.

문제에 주어지는 제약 조건은 다음과 같다.

### **[제한사항]**

- play_time, adv_time은 길이 8로 고정된 문자열입니다.
    - play_time, adv_time은 `HH:MM:SS` 형식이며, `00:00:01` 이상 `99:59:59` 이하입니다.
    - 즉, 동영상 재생시간과 공익광고 재생시간은 `00시간 00분 01초` 이상 `99시간 59분 59초` 이하입니다.
    - 공익광고 재생시간은 동영상 재생시간보다 짧거나 같게 주어집니다.
- logs는 크기가 1 이상 300,000 이하인 문자열 배열입니다.
    - logs 배열의 각 원소는 시청자의 재생 구간을 나타냅니다.
    - logs 배열의 각 원소는 길이가 17로 고정된 문자열입니다.
    - logs 배열의 각 원소는 `H1:M1:S1-H2:M2:S2` 형식입니다.
        - `H1:M1:S1`은 동영상이 시작된 시각, `H2:M2:S2`는 동영상이 종료된 시각을 나타냅니다.
        - `H1:M1:S1`는 `H2:M2:S2`보다 1초 이상 이전 시각으로 주어집니다.
        - `H1:M1:S1`와 `H2:M2:S2`는 play_time 이내의 시각입니다.
- 시간을 나타내는 `HH, H1, H2`의 범위는 00~99, 분을 나타내는 `MM, M1, M2`의 범위는 00~59, 초를 나타내는 `SS, S1, S2`의 범위는 00~59까지 사용됩니다. 잘못된 시각은 입력으로 주어지지 않습니다. (예: `04:60:24`, `11:12:78`, `123:12:45` 등)
- return 값의 형식
    - 공익광고를 삽입할 시각을 `HH:MM:SS` 형식의 8자리 문자열로 반환합니다.

---

## 🤔 풀이 고민

### 찾아본 풀이

찾아본 풀이는 다음과 같다.

공익 광고 노출에 적절한 시간은 가장 많은 시청자 수를 기록한 구간이다. 즉, 누적 시청자 수가 최대가 되는 구간을 파악하기 위해서는 누적합을 적용해야한다. 누적합이란, 현재 위치의 값을 현재 위치의 값과 이전까지의 누적한 값을 더하는 방식을 의미한다.

문자열로 주어진 시간을 그대로 사용하기 어렵기 때문에 기본 단위인 초로 바꿔서 정수 형태로 변환한다. DP테이블의 1칸을 1초 단위로 취급해서 구성한다. 그리고 `logs` 를 순회하며 각 사용자별 시청 시작 시각과 종료 시각에 +1, -1을 입력하여 시청자 수가 1명 증가했음과 1명 감소했음을 표시한다.

시청 구간 내에 기록을 안하는 이유는 누적합 때문이다. 구간 안에도 시청자 수를 표시하려하면 이 작업을 위한 반복문이 추가로 필요하다. 누적합은 구간의 처음과 끝 부분에 증가 및 감소를 기록하고, DP테이블을 처음부터 끝까지 순회하며 각 초 단위로 몇 명의 시청자가 있는지 알 수 있어 불필요한 반복문 사용을 하지 않아도 된다.

가령 아래의 예시에서, 시청 구간의 시작 시간과 종료 시간에 +1, -1을 더하면, `[2, 1, 0, 0, -1, -1, 1, -2]` 이 된다. 이제, `i` 번째 칸을 `i-1` 번째 칸과의 합으로 갱신하면, 즉 누적합을 수행하면, `[2, 3, 3, 3, 2, 1, 2, 0]` 이 된다. 기존 `logs` 와 비교해보면, 특정 시간에 몇 명이 시청하고 있는지를 대응해볼 수 있다.

현재 DP테이블은 각 시간별 시청자 수를 담고 있다. 여기에서 누적합을 한 번 더 수행하면, 구간별 누적 시청자 수를 구할 수 있게 된다. DP테이블에서 `00:00:00` 부터 공익 광고를 편성할 수 있는 마지막 시간까지 1초씩 이동하며, 현재 시각에 공익 광고를 삽입했을 때의 누적 시청자 수와 기존 최대 누적 시청자 수를 비교한다. 이 방식으로 최종적인 최대 누적 시청자 수를 기록한 구간의 시작 시간을 구할 수 있게 된다.

https://www.slideshare.net/slideshows/killingcamp-killingcamp/266413316

### 결론

- DP & 구간합 & 문자열 테크닉을 활용하는 문제이다.

---

## 🏃 코드 작성 과정

### 찾아본 풀이로 작성한 코드

```python
def solution(play_time, adv_time, logs):
    # 메서드
    def str_to_int(target):
        h, m, s = map(int, target.split(":"))
        return h * 3600 + m * 60 + s
    
    def int_to_str(target):
        h = str(target // 3600) if target // 3600 >= 10 else "0" + str(target//3600)
        target %= 3600    
        m = str(target // 60) if target // 60 >= 10 else "0" + str(target // 60)
        s = str(target % 60) if target % 60 >= 10 else "0" + str(target % 60)
        
        return h + ":" + m + ":" + s
    
    # 메인 로직
    answer = 0
    
    #✅ 문자열로 주어진 시간을 정수형 단위시간(초)으로 변환하기
    unit_play_time, unit_adv_time = str_to_int(play_time), str_to_int(adv_time)
    dp = [0] * (unit_play_time + 1)
    
    for log in logs:
        start_time, end_time = log.split("-")
        start_time, end_time = str_to_int(start_time), str_to_int(end_time)
        
        #✅ 시작시간에 +1, 종료시간에 -1
        dp[start_time] += 1
        dp[end_time] -= 1
    
    #✅ DP테이블을 순회하며 기록된 정보를 통해 구간별 시청자 수를 기록한다.
    for i in range(1, unit_play_time):
        dp[i] = dp[i] + dp[i - 1]
    
    #✅ DP테이블을 순회하며 구간별 시청자 수를 통해 누적 시청자 수를 기록한다.
    for i in range(1, unit_play_time):
        dp[i] = dp[i] + dp[i - 1]
    
    #✅ DP테이블을 순회하며 광고 시간동안 누적 시청자 수가 가장 많은 구간을 구한다.
    result = dp[unit_adv_time - 1]
    for i in range(0, unit_play_time + 1 - unit_adv_time):
        if result < dp[i + unit_adv_time] - dp[i]:
            result = dp[i + unit_adv_time] - dp[i]
            answer = i + 1
    
    return int_to_str(answer)
```

---

## 🥰 배운점 & 느낀점

- 이 문제는 실제 카카오 블라인드 채용 문제인데 정답률이 1%대라고 한다.
- 이런 문제를 내가 혼자서 풀 수 있을지.. 겁이난다.
- 그래도 우선 이 문제라도 제대로 복습하면서 체화할 필요가 있을거 같다.